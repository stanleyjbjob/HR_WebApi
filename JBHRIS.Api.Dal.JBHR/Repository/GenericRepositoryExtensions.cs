using AutoMapper;
using System;
using System.Collections.Generic;
using System.Linq;
using System.Text;

namespace JBHRIS.Api.Dal.JBHR.Repository
{
    public static class GenericRepositoryExtensions
    {
    //    public static IQueryable<TEntityDto> GetCurrentOnJob<TEntity, TEntityDto>(this IRepository<TEntity> repository, List<string> employeeList, DateTime BeginDate, DateTime EndDate)
    //where TEntity : class
    //    {
    //        var mapperConfig = new MapperConfiguration(
    //            cfg => cfg.CreateMap<TEntity, TEntityDto>()
    //            );

    //    }
    }
}
